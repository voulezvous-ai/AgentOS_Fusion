# Core Framework
fastapi[all]>=0.110.0,<1.0.0          # Web framework moderno com extras (docs, pydantic, etc)
uvicorn[standard]>=0.29.0,<1.0.0      # ASGI server com suporte a reload e produção

# MongoDB Async
motor>=3.4.0,<4.0.0                   # Driver async para MongoDB 

# Tarefas Assíncronas
celery>=5.3.6,<6.0.0                  # Task queue assíncrona
redis>=5.0.1,<6.0.0                   # Broker e cache backend

# Ambiente e Configuração
python-dotenv>=1.0.0,<2.0.0           # Carrega variáveis do .env
pydantic-settings>=2.2.0,<3.0.0       # Configuração baseada em Pydantic v2

# Logs e Observabilidade
loguru>=0.7.2,<1.0.0                  # Logger moderno com sintaxe simples
# sentry-sdk[fastapi,celery,redis]>=1.45.0,<2.0.0  # (opcional) Monitoramento de erros

# Segurança e Autenticação
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT + criptografia
passlib[bcrypt]>=1.7.4,<2.0.0            # Hash de senha seguro

# Redes e APIs
httpx>=0.27.0,<1.0.0                 # Cliente HTTP async

# OpenAI e IA
openai>=1.16.0,<2.0.0                # Cliente OpenAI API
# google-generativeai>=0.5.2,<1.0.0  # (opcional) Gemini API

# Utilitários
bson>=0.5.10                         # Suporte direto ao ObjectId do Mongo (não incluso em stdlib)
pytz>=2024.1                         # Timezones (legacy, se necessário)

# Cache e Limites de Rate
fastapi-cache2[redis]>=0.2.1         # Cache para endpoints
fastapi-limiter>=0.1.5               # Rate limiting baseado em Redis

# ⚠️ Atenção: `uuid` é builtin no Python 3 — não é necessário adicionar ao requirements.

